# Environment Variables Configuration
# Copy this file to .env.local and fill in your actual values

# =============================================================================
# CLIENT-SIDE VARIABLES (NEXT_PUBLIC_*)
# These are safe to expose to the browser
# =============================================================================

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Feature Flags
NEXT_PUBLIC_E2E_TEST=false
NEXT_PUBLIC_IMPORT_V2_ENABLED=true

# =============================================================================
# SERVER-ONLY VARIABLES
# These are NEVER exposed to the client and should be kept secret
# =============================================================================

# Supabase Service Role (for admin operations)
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Stripe Configuration
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret
STRIPE_PRICE_BASIC=price_your-basic-price-id
STRIPE_PRICE_PRO=price_your-pro-price-id

# Optional: Sentry DSN for error tracking
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================

# 1. Never commit .env.local to version control
# 2. Server-only variables (without NEXT_PUBLIC_) are automatically excluded from client bundle
# 3. Use the centralized env system in src/lib/env.ts for type-safe access
# 4. Run `npm run validate-env` to check all required variables are set
# 5. Run `npm run check-secrets` to ensure no server secrets leak to client
