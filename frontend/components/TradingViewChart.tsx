// TradingViewChart temporarily removed due to persistent type/build issues with lightweight-charts integration.
// You can re-implement this with a different charting library or after resolving the version/typings mismatch.

export default function TradingViewChart() {
  return (
    <div className="w-full h-80 bg-background rounded flex items-center justify-center text-muted-foreground">
      Chart temporarily unavailable
    </div>
  );
}
